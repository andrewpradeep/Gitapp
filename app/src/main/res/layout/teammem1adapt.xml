<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="7dp">
    <android.support.v7.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="120dp"
        app:cardCornerRadius="10dp"
        app:cardElevation="10dp"
        android:padding="7dp">
        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@color/colordec">
    <ImageView
        android:layout_width="60dp"
        android:layout_height="60dp"
        android:id="@+id/img_bat"
        android:src="@drawable/cric"
        android:scaleType="centerCrop"
        android:padding="8dp"
        android:layout_marginTop="3dp"
        />
         <EditText
         android:layout_width="match_parent"
         android:layout_height="40dp"
         android:hint="@string/hintsize"
         android:textSize="10sp"
         android:id="@+id/edit_playname"
         android:layout_alignBottom="@+id/img_bat"
         android:layout_toRightOf="@+id/img_bat"
         android:layout_toEndOf="@+id/img_bat" />
        </RelativeLayout>
    </android.support.v7.widget.CardView>

</LinearLayout>